<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cloudbase.mapper.UserMapper">

    <resultMap id="UserResultMap" type="com.cloudbase.entity.User">
        <id column="id" property="id" />
        <result column="name" property="name" />
        <result column="employee_id" property="employeeId" />
        <result column="email" property="email" />
        <result column="phone" property="phone" />
        <result column="password" property="password" />
        <result column="department_id" property="departmentId" />
        <result column="status" property="status" />
        <result column="created_at" property="createdAt" />
        <result column="updated_at" property="updatedAt" />
        <result column="last_login_at" property="lastLoginAt" />
    </resultMap>

    <!-- 根据部门ID查询用户列表 -->
    <select id="selectByDepartmentId" resultMap="UserResultMap">
        SELECT * FROM auth_user WHERE department_id = #{departmentId}
        ORDER BY id ASC
    </select>

    <!-- 根据姓名或员工ID模糊查询用户列表 -->
    <select id="selectByNameOrEmployeeId" resultMap="UserResultMap">
        SELECT * FROM auth_user
        WHERE name LIKE CONCAT('%', #{name}, '%') OR employee_id LIKE CONCAT('%', #{employeeId}, '%')
        ORDER BY id ASC
    </select>

    <!-- 根据姓名或员工ID模糊查询并按部门ID过滤用户列表 -->
    <select id="selectByNameOrEmployeeIdAndDepartmentId" resultMap="UserResultMap">
        SELECT * FROM auth_user
        WHERE (name LIKE CONCAT('%', #{name}, '%') OR employee_id LIKE CONCAT('%', #{employeeId}, '%'))
        AND department_id = #{departmentId}
        ORDER BY id ASC
    </select>

    <!-- 根据姓名或员工ID查询单个用户 -->
    <select id="selectOneByNameOrEmployeeId" resultMap="UserResultMap">
        SELECT * FROM auth_user
        WHERE name = #{name} OR employee_id = #{employeeId}
        LIMIT 1
    </select>

    <!-- 根据条件查询用户列表 -->
    <select id="selectUserList" resultMap="UserResultMap">
        SELECT * FROM auth_user
        <where>
            <if test="params.keyword != null and params.keyword != ''">
                AND (name LIKE CONCAT('%', #{params.keyword}, '%') OR employee_id LIKE CONCAT('%', #{params.keyword}, '%'))
            </if>
            <if test="params.departmentId != null">
                AND department_id = #{params.departmentId}
            </if>
        </where>
        ORDER BY id ASC
    </select>

</mapper>
