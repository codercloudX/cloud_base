<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cloudbase.mapper.DepartmentMapper">

    <resultMap id="DepartmentResultMap" type="com.cloudbase.entity.Department">
        <id column="id" property="id" />
        <result column="name" property="name" />
        <result column="description" property="description" />
        <result column="parent_id" property="parentId" />
        <result column="status" property="status" />
        <result column="created_at" property="createdAt" />
        <result column="updated_at" property="updatedAt" />
        <result column="level" property="level" />
    </resultMap>

    <!-- 根据条件查询部门列表 -->
    <select id="selectDepartmentList" resultMap="DepartmentResultMap">
        SELECT * FROM auth_department
        <where>
            <if test="params.keyword != null and params.keyword != ''">
                AND name LIKE CONCAT('%', #{params.keyword}, '%')
            </if>
        </where>
        ORDER BY id ASC
    </select>

    <!-- 查询所有部门 -->
    <select id="selectAllDepartments" resultMap="DepartmentResultMap">
        SELECT * FROM auth_department
        ORDER BY id ASC
    </select>

</mapper>
